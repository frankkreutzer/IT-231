<%= form_for(reorder) do |f| %>
  <% if reorder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reorder.errors.count, "error") %> prohibited this reorder from being saved:</h2>

      <ul>
      <% reorder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %></br>
    <%= f.number_field :product_id %>
  </div></br>

  <div class="field">
    <%= f.label :supplier_id %></br>
    <%= f.number_field :supplier_id %>
  </div></br>

  <div class="field">
    <%= f.label :qtyorder %></br>
    <%= f.number_field :qtyorder %>
  </div></br>

  <div class="field">
    <%= f.label :reorderdate %></br>
    <%= f.date_select :reorderdate %>
  </div></br>

  <div class="field">
    <%= f.label :expecteddelivery %></br>
    <%= f.date_select :expecteddelivery %>
  </div></br>

  <div class="actions">
    <%= f.submit %>
  </div></br>
<% end %>
