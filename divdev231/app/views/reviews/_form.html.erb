</br><%= form_for(review) do |f| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :customer_id %>
  </div>

  <div class="field">
    <%= f.hidden_field :product_id %>
    <%= Product.find(@prod_id).productname %>
  </div></br>

  <div class="field">
    <%= f.hidden_field :reviewdate %>
  </div></br>

  <div class="field">
    <%= f.label :comments %></br>
    <%= f.text_area :comments, :size=>"50x10" %>
  </div></br>

  <div class="field">
    <%= f.label :rating %></br>
    <%= f.select :rating, "<option>1</option> <option>2</option>  <option>3</option>  <option>4</option>  <option>5</option>".html_safe %>
  </div></br>

  <div class="actions">
    <%= f.submit %>
  </div></br>
<% end %>
