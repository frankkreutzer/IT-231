<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
     <%= f.label :customer_id %></br>
     <%= f.select :customer_id, @customer_choice %>
   </div></br>

  <div class="field">
    <%= f.label :saledate %></br>
    <%= f.date_select :saledate %>
  </div></br>

  <div class="field">
    <%= f.label :salenotes %></br>
    <%= f.text_area :salenotes, :size=>"50x10" %>
  </div></br>

  <div class="actions">
    <%= f.submit %>
  </div></br>
<% end %>
